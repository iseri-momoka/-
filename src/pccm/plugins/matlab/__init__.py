"""MATLAB-port plugins — algorithms ported from MATLAB.

Each sub-package (e.g. ``ground_filter_csf``) is a standalone plugin
with its own ``algorithm.py`` that registers via ``pccm.plugins.api.register``.
"""
